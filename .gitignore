# macOS / external volume junk (never commit)
._*
.DS_Store
.Rhistory

# GitHub SSH private key (stored locally for this machine; never commit)
github-ssh/id_ed25519_digital_ripples

# Node (dual-display relay server)
node_modules/

# Video binaries (large; copy locally — expected under assets/videos/)
*.mp4
*.webm
*.mov
*.m4v
*.mkv
